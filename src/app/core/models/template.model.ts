export interface TemplateConfig {
    id: string;
    name: string;
    description: string;
    primaryColor: string;
    fontFamily: string;
    layoutType: string;
    isAtsOptimized?: boolean;
    thumbnailUrl?: string;
}
